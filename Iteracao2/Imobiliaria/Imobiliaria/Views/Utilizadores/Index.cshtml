@model IEnumerable<Imobiliaria.Models.Utilizador>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PrimeiroNome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UltimoNome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PrimeiroNome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UltimoNome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UtilizadorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UtilizadorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UtilizadorID })
        </td>
    </tr>
}

</table>
